# AGENT SYSTEM DIRECTIVE: ProductLogik Implementation

**CONTEXT & COMPLIANCE**: 
- **Tech Stack**: React 18 (Vite/TS/Tailwind) Frontend + FastAPI (Python/Supabase) Backend. 
- **Determinism**: Execute tasks sequentially. Do NOT write unnecessary logic. Do NOT refactor existing features outside the scope of these targets.
- **STRICT EXECUTION OVERRIDE**: You are explicitly forbidden from skipping, omitting, or ignoring any `[ ]` task listed below. You must complete every single item in order. The **ONLY** valid exception to this rule is if completing a specific task would directly trigger a critical web application vulnerability, a severe application crash, or violate user data privacy. If you bypass a task under this exception, you must immediately halt the process and alert the user with your reasoning.
- **ACCOUNTABILITY TRACKING**: You must actively edit this current markdown file (`docs/implementation_plan_09042026.md`) as you work. Immediately mark an incomplete task `[ ]` as completed `[x]` the exact moment you finish the associated code changes, before moving on to the next task.

---

## TASK 1: MISSING ROUTE STRUCTURE
**TARGETS**: `frontend/src/pages/`, `frontend/src/App.tsx`
- [x] Create 10 `.tsx` files in `frontend/src/pages/`: `MethodologyPage.tsx`, `ContactPage.tsx`, `PrivacyPolicyPage.tsx`, `TermsOfServicePage.tsx`, `IntegrationsPage.tsx`, `EnterprisePage.tsx`, `CaseStudiesPage.tsx`, `BlogPage.tsx`, `HelpCenterPage.tsx`, `CookiePolicyPage.tsx`.
- [x] **Implementation Rule (Informational Pages)**: For 9 of these pages, scaffold a generic React functional component returning a centered container with a Title header and generic `Lorem Ipsum` placeholder paragraphs matching existing typographic styles.
- [x] **Implementation Rule (`ContactPage.tsx`)**: Build a visual form (Name, Email, Message) using `<form onSubmit={e => { e.preventDefault(); }}>`. Render a `sonner` success toast on submit. Do NOT wire to any backend API.
- [x] Modify `frontend/src/App.tsx`: Import all 10 created components.
- [x] Modify `frontend/src/App.tsx`: Append strict `<Route>` definitions linking to the respective placeholder components.
- [x] Modify `frontend/src/App.tsx`: If a dynamic titles hash/map exists, append the 10 new routes to it.

## TASK 2: FOOTER REFACTORING
**TARGETS**: `frontend/src/components/layout/Footer.tsx`
- [x] Refactor `<Link to="#" >` tags. Map the paths directly to the React Router paths established in Task 1.
- [x] Import `lucide-react` icons (Twitter, LinkedIn, GitHub). Locate the empty placeholder `div` blocks in the social section and replace them with the parsed icons.
- [x] Wrap the newsletter `<Input>` and `<Button>` into an encapsulating `<form onSubmit={...}>` block. 
- [x] Prevent default form submission behaviour to avoid page refresh, and execute a `sonner` toast notification (`Thanks for subscribing!`).

## TASK 3: PDF EXPORT SERVICE [BACKEND]
**TARGETS**: `backend/services/pdf_service.py` (Create/Modify), `backend/requirements.txt`, `backend/analysis.py`
- [x] Modify `backend/requirements.txt`: Append `reportlab` or `fpdf2`.
- [x] Create or Update `backend/services/pdf_service.py` constructing a primary entry method: `generate_report(analysis_data, db_tier)`.
- [x] **Formatting Logic**: Render Cover Page (Title/Branding) -> `executive_summary` string block -> Iterate `themes_json` arrays rendering blocks consisting of (Theme Name, Count integer, Evidence quotes string array).
- [x] **Tier Logic Override**: `IF db_tier == "demo"`, inject a diagonal translucent text string (`Generated by ProductLogik.com Demo`) across the generated buffer canvas.
- [x] Modify `backend/analysis.py`: Guarantee the `/api/analysis/{upload_id}/export` endpoint natively builds the binary PDF buffer and resolves using `StreamingResponse(buffer, media_type="application/pdf")`.

## TASK 4: PDF EXPORT INTEGRATION [FRONTEND]
**TARGETS**: `frontend/src/pages/ResultsPage.tsx`, `frontend/src/lib/api.ts`
- [x] Modify `frontend/src/lib/api.ts`: Add `exportAnalysis(uploadId: string, token: string)` handler specifically parsing a `.blob()` response rather than `.json()`.
- [x] Modify `frontend/src/pages/ResultsPage.tsx`: Inject an explicit "Export to PDF" `<Button>` adjacent to the existing theme result headers.
- [x] Bind "Export to PDF" Action: React standard state loading sequence -> execute `api.exportAnalysis()` -> construct `window.URL.createObjectURL(blob)` -> simulate invisible `<a>` DOM click for file download -> clear sequence state.

## TASK 5: PRODUCTION VALIDATION PARAMETERS
**TARGETS**: `backend/main.py`, `.env` constraints
- [x] **Integrity Rule**: Execution of `uvicorn main:app` must bind cleanly without internal process crashing.
- [x] Ensure `fastapi.middleware.cors` dynamically relies on `os.getenv("FRONTEND_URL")` rather than hardcoded string domains.
*(Hostinger Migration Context Override: The production environment will utilize proxy routing where frontend runs statically on PM2 `:3001` and backend maps directly via PM2 `:8081`. Ignore Vercel/Render edge cases while verifying code logic.)*

---

## EXECUTION VERIFICATION
- [x] `npm run build` execution -> Validates with 0 strict formatting or TS compilation errors.
- [x] Native click interaction inside `Footer.tsx` renders new placeholder pages securely without `404` boundaries.
